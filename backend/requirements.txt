# ---- Web framework / async runtime --------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1
aiosqlite==0.20.0

# ---- Core ML / diffusion stack --------------------------------------------
# Pinned to a diffusers release line known to ship LTXPipeline, WanPipeline,
# HunyuanVideoPipeline, and StableVideoDiffusionPipeline simultaneously.
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1
diffusers==0.32.2
transformers==4.47.1
accelerate==1.2.1
safetensors==0.4.5
sentencepiece==0.2.0
einops==0.8.0
imageio==2.36.1
imageio-ffmpeg==0.5.1
huggingface-hub==0.27.0

# ---- TTS -------------------------------------------------------------------
TTS==0.22.0           # Coqui XTTS-v2
piper-tts==1.2.0
soundfile==0.12.1

# ---- Image / video IO --------------------------------------------------------
Pillow==11.0.0
numpy==1.26.4
opencv-python-headless==4.10.0.84

# ---- Narration (optional LLM path) --------------------------------------------
anthropic==0.42.0

# ---- Utilities ---------------------------------------------------------------
python-dotenv==1.0.1
loguru==0.7.3

# NOTE: ffmpeg and ffprobe are NOT pip packages — they are system binaries.
# Install via your OS package manager (see installation guide). imageio-ffmpeg
# above only vendors a CLI binary used as a fallback by some imageio code
# paths; the project's own ffmpeg_service.py and video_io.py shell out to the
# system `ffmpeg`/`ffprobe` directly and require them on PATH.
